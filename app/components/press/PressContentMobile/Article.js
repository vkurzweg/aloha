import styled from 'styled-components';

const Article = styled.div`
  display: inline-flex;
  margin-top: 4%;
  padding: 2%;

   &:hover, &:active, &:focus {
     color: #FF80AB;
  }
`;

export default Article;
