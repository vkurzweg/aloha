import styled from 'styled-components';

const Article = styled.div`
  padding: 1%;
  display: inline-flex;
  margin-top: 4%;
  margin-left: 3%;

   &:hover, &:active, &:focus {
     color: #FF80AB;
  }
`;

export default Article;
