// import { fromJS } from 'immutable';
// import { makeSelectCareersPageDomain } from '../selectors';

// const selector = makeSelectCareersPageDomain();

describe('makeSelectCareersPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
