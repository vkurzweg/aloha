// import { fromJS } from 'immutable';
// import { makeSelectGalleryPageDomain } from '../selectors';

// const selector = makeSelectGalleryPageDomain();

describe('makeSelectGalleryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
